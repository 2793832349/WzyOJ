# TURN server configuration for WzyOJ Live

# 监听所有接口
listening-ip=0.0.0.0

# 监听端口
listening-port=3478
tls-listening-port=5349

# 外部 IP（服务器公网 IP）
external-ip=154.12.80.16

# 域名
realm=kc.matrixcoding.top

# 用户认证
user=wzyoj:wzyoj2024turn

# 日志
log-file=/var/log/turnserver.log
verbose

# 安全设置
fingerprint
lt-cred-mech

# 中继端口范围
min-port=49152
max-port=65535

# 禁用 CLI
no-cli

# 禁用 TLS（如果没有证书）
# 如果有证书，可以启用
# cert=/etc/letsencrypt/live/kc.matrixcoding.top/fullchain.pem
# pkey=/etc/letsencrypt/live/kc.matrixcoding.top/privkey.pem
